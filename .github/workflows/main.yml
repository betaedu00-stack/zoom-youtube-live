name: Zoom to YouTube Stream

on:
  workflow_dispatch:

jobs:
  stream:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Google Chrome & Tools
        run: |
          sudo apt-get update
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable xvfb ffmpeg pulseaudio fluxbox

      - name: Install NPM Dependencies
        run: |
          # මෙහිදී Puppeteer අමතරව Chrome බාගත කිරීම නවත්වයි (වැඩේ වේගවත් වේ)
          export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
          npm install

      - name: Start Stream
        env:
          ZOOM_URL: ${{ secrets.ZOOM_URL }}
          YOUTUBE_STREAM_KEY: ${{ secrets.YOUTUBE_STREAM_KEY }}
        run: |
          # Virtual Display එක සෑදීම
          Xvfb :99 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset &
          export DISPLAY=:99
          
          fluxbox &
          sleep 5

          # Audio Setup
          pulseaudio -D --exit-idle-time=-1
          pacmd load-module module-virtual-sink sink_name=vss
          pacmd set-default-sink vss

          # Zoom Script එක පණගැන්වීම
          node zoom.js &
          
          echo "Waiting for Zoom to initialize (60s)..."
          sleep 60

          # FFmpeg හරහා YouTube වෙත Stream කිරීම
          ffmpeg -f x11grab -draw_mouse 0 -video_size 1920x1080 -i :99 \
          -f pulse -i default \
          -c:v libx264 -preset veryfast -b:v 3000k -maxrate 3000k -bufsize 6000k \
          -pix_fmt yuv420p -g 60 \
          -c:a aac -b:a 128k -ar 44100 \
          -f flv "rtmp://a.rtmp.youtube.com/live2/${YOUTUBE_STREAM_KEY}"
